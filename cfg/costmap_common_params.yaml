footprint: [[0, 0], [0.6, 0], [0.6, 0.4], [0.0, 0.4] ]

obstacle_layer:
  enabled:              true
  unknown_threshold:    15
  mark_threshold:       0
  combination_method:   1
  track_unknown_space:  true
  obstacle_range: 8.5
  raytrace_range: 3.0
  observation_sources:  scan 
  scan:
    data_type: LaserScan
    topic: /scan
    marking: true
    clearing: true
    
inflation_layer:
  enabled:              true
  cost_scaling_factor:  10.0
  inflation_radius:     0.5

static_layer:
  enabled:              true
